import { PainelDoCorretor } from '@/components/painel-do-corretor'

/** A porta do app: quanto o corretor vai receber, e quando. */
export default function AppPage() {
  return <PainelDoCorretor />
}
